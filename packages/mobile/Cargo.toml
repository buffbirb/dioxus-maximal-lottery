[package]
edition = "2024"
name = "mobile"
version = "0.1.0"

[dependencies]
dioxus = { features = ["fullstack", "router"], workspace = true }
ui = { workspace = true }

[features]
default = []
mobile = ["dioxus/mobile"]
server = ["dioxus/server", "ui/server"]
